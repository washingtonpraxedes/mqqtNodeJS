{
  "name": "mqtt_node_api",
  "version": "1.0.0",
  "description": "Node APP for mqtt/ionic integration",
  "main": "mqtt.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hari": "^0.1.1",
    "mqtt": "^2.0.1"
  }
}
